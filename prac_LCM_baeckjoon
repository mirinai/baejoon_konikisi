#include <stdio.h>

void LCM(int max, int min,int T) {
	
	

}
int GCdivisor(int x, int y) {

}


int main(void) {

	//테스트 케이스의 개수 T(1 ≤ T ≤ 1,000)
	int T;
	//printf("두 정수 입력(띄어쓰기) :");
	scanf("%d", &T);

	//입력을 받아낼 array
	int arr[1000][2];



	//값을 받아낼 변수
	//nt max = 0, min = 0;

	//최솟값배열
	int least_common_multiple[1000];

	//입력
	for (int i = 0; i < T; i++) {
		//printf("두 정수 입력(띄어쓰기) :");
		scanf("%d %d", &arr[i][0], &arr[i][1]);

		//1이 있는 경우
		/*if (arr[i][0] == 1) {
			least_common_multiple[i] = arr[i][1];

		}
		else if (arr[i][1] == 1) {
			least_common_multiple[i] = arr[i][0];
		}

		else if (arr[i][0] > arr[i][1]) {
			max = arr[i][0];
			min = arr[i][1];
			
		}
		else {
			max = arr[i][1];
			min = arr[i][0];
			
		
		}
		maxMin(max, min, T);*/



	}
	for (int i = 0; i < T; i++) {
		printf("%d\n", least_common_multiple[i]);
	}




	return 0;

}
